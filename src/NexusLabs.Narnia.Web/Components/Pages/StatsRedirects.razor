@page "/activity"
@page "/analytics/activity"
@page "/hot-files"
@page "/analytics/hot-files"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        var source = new Uri(Navigation.Uri);
        Navigation.NavigateTo($"/stats{source.Query}", replace: true);
    }
}
