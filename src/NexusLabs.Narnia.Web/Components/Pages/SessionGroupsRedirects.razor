@page "/session-groups"
@page "/groups"
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/collections?from=session-groups", replace: true);
    }
}
